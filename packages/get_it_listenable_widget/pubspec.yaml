name: get_it_listenable_widget
description: "A ListenableWidget that resolves its ViewModel from GetIt, with optional factory params."
version: 0.1.0
homepage: "https://seg.veenstra.dev"
repository: "https://github.com/SEGVeenstra/flutter_listenable_widget/tree/master/packages/get_it_listenable_widget"

environment:
  sdk: ^3.8.1
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  listenable_widget: ^0.2.0
  get_it: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
